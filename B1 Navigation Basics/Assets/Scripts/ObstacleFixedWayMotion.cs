using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObstacleFixedWayMotion : MonoBehaviour
{

    NavMeshObstacle bottleneckObstacle;
    float distance;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
        if(bottleneckObstacle.enabled){
            
        }
        
    }
}
